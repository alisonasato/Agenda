# WhatsappTemplates

`src/components/sites/eagenda-com-br-a1f95f96/notificacao-whatsapp_template-d74ae14b/WhatsappTemplates.tsx`

- The listing is built like Modelos de Email, with its own columns and empty state.
- The modal uses the shared `Modal` (lg) and `Combobox` (`required`, `searchInPopover`).
- New shared icon: `ChatBubbleIcon`, the bubble without lines, used by the empty state.
