# AgendaAdmin

`src/components/sites/eagenda-com-br-a1f95f96/painel-controle-3afc63eb/AgendaAdmin.tsx`

- **AgendaAdmin:** `tool` (the open modal) and `query` (the search).
- **ToolModal:** switches on the tool id and builds each modal from local parts: `AgendaTargets`, `Period`, `DateField`, `TimeField`, `NumberField`, `RadioPills`, `Footer`.
- **EmptyTable / Empty:** the two empty tables.
